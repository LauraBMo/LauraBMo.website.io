#+TITLE: laurabmo.com — rebuild progress
#+AUTHOR: Laura Brustenga i Moncusí
#+STARTUP: overview logdone
#+TODO: TODO(t) NEXT(n) WAIT(w@/!) | DONE(d!) CANCELLED(c@)
#+PRIORITIES: A C B
#+FILETAGS: :website:
#+COLUMNS: %40ITEM(Task) %TODO %PRIORITY %TAGS %EFFORT(Est){:}
#+CATEGORY: web

* Context
:PROPERTIES:
:CREATED:  [2026-08-04]
:BRANCH:   rebuild
:END:

Rebuild of an old theme-based Hugo site into a hand-built four-section site.

*Live at https://laurabmo.com/ since [2026-08-05].* Push to =main= builds at the pinned
Hugo version with warnings as errors, then deploys to a Cloudflare Worker. What remains
is content, not plumbing.

Audiences, in priority order:
1. Academic/industry bio + CV — job search, the reason for the deadline pressure
2. SoftReminder landing + privacy policy
3. Personal writing / autobiographical essays
4. Therapy/helper promotion

Dates below record when an item was logged in this file, not necessarily when the
work happened — several DONE items predate it.

* DONE Structural rebuild                                        :hugo:infra:
CLOSED: [2026-08-04]
:PROPERTIES:
:CREATED:  [2026-08-04]
:END:

** DONE Diagnose the dead site
CLOSED: [2026-08-04]
:PROPERTIES:
:ROOT_CAUSE: theme submodule + =.Site.IsServer= removed in Hugo 0.120
:END:
CI was pinned to Hugo 0.86.1, so a template calling =.Site.IsServer= kept working
in CI for years while the repo itself was broken. The pin hid the rot rather than
preventing it — the argument for =--panicOnWarning=.

** DONE Remove the theme, hand-write layouts                          :hugo:
CLOSED: [2026-08-04]
:PROPERTIES:
:FILES: layouts/{baseof,home,single,list}.html, layouts/_partials/, layouts/_markup/
:END:
Submodule deinit'd, =.gitmodules= deleted, =themes/= gone.

** DONE Single stylesheet, Hugo Pipes + SRI                            :css:
CLOSED: [2026-08-04]
=assets/css/main.css=, ~220 lines, light/dark via =prefers-color-scheme=.

** DONE Build-time LaTeX, no runtime JS                          :hugo:latex:
CLOSED: [2026-08-04]
:PROPERTIES:
:APPROACH: Goldmark passthrough + transform.ToMath, output=htmlAndMathml
:VENDORED: assets/css/vendor/katex.min.css (0.16.22) + static/fonts/*.woff2 (20 files)
:END:
Stylesheet emitted only on the two pages that actually contain math.
Chosen over runtime KaTeX/MathJax so nothing loads at runtime and no CDN is touched.

** DONE Content stubs and nav for all sections                      :content:
CLOSED: [2026-08-04]
=_index.md=, =cv.md=, =research/=, =softreminder/=, =writing/=. Each carries a =STUB=
comment distinguishing placeholder from sourced text.

** DONE Renegotiate the CLAUDE.md rules                                :docs:
CLOSED: [2026-08-04]
Walked every rule with pros and cons. The file had been pasted from an earlier chat,
so its rules were being followed more strictly than they had ever been chosen.

* DONE Domain and hosting                                    :domain:cloudflare:
CLOSED: [2026-08-04]

** DONE Establish that laurabmo.xyz is gone                          :domain:
CLOSED: [2026-08-04]
:PROPERTIES:
:EVIDENCE: whois — created 2026-06-21, registrar NameCheap, NS on Cloudflare
:END:
Renewal was stopped deliberately in 2023 to end an online presence. The domain was
re-registered by a third party on 2026-06-21 and is held until at least 2027-06-21.
Not recoverable at sensible cost.

** DONE Choose laurabmo.com                                          :domain:
CLOSED: [2026-08-04]
Considered =.xyz= again (the =x, y, z= joke is good), =.me=, and =.cat=.
=.com= won on neutrality for the job search and on email deliverability —
=.xyz= carries enough spam reputation to hurt mail from the domain.

** DONE Repo side of the Cloudflare Pages move                   :cloudflare:
CLOSED: [2026-08-04]
:PROPERTIES:
:FILES: .github/workflows/deploy.yml, .env, static/CNAME
:END:
=peaceiris/actions-gh-pages= → =cloudflare/wrangler-action=. Hugo still builds in the
workflow, not in Cloudflare's build system, so the version pin and =--panicOnWarning=
stay in the repo. =static/CNAME= deleted; =GIT_DEPLOY_BRANCH= → =CF_PAGES_PROJECT=.

** DONE Analytics decision and implementation                 :cloudflare:privacy:
CLOSED: [2026-08-04]
:PROPERTIES:
:FILES: layouts/_partials/analytics.html, config/_default/params.toml
:BLOCKED_ON: token — see [[*NEXT Paste the Web Analytics token]]
:END:
Cloudflare Web Analytics: cookieless, per-path views, bot filtering. The single
exemption to both "no runtime JS" and "no third-party assets" — justified because
Cloudflare already terminates TLS for every request, so no new party sees visitors.
Gated on =hugo.IsProduction= and a non-empty token, so an empty token is a full off switch.

** DONE Hugo 0.148.2 → 0.162.1                                    :hugo:infra:
CLOSED: [2026-08-04]
:PROPERTIES:
:BREAKING: languageCode → locale; .Language.LanguageCode → .Language.Locale
:END:
Both deprecated in 0.158.0, neither backward compatible — verified that the fixed
templates fail on 0.148.2. Local and CI must therefore move in the same commit.

** DONE Drop Docker/Traefik for native Hugo                           :infra:
CLOSED: [2026-08-04]
:PROPERTIES:
:FILES: docker-compose.yml (deleted), Makefile, README.md, .env
:END:
The compose file dated from 2022, ran Traefik v1, and mounted the Docker socket into
a container — root-equivalent exposure, to buy a prettier hostname than =localhost:1313=.
Fedora ships Hugo extended; containers bought nothing.

** DONE Remove the inherited GPL v3 licence                           :legal:
CLOSED: [2026-08-04]
The repo began as a Hugo template by Aran Moncusí Ramírez. After the rebuild nothing
of that template remained, so the licence file went with it.
Note: the repo now carries *no* licence, which defaults to all rights reserved.

* DONE Cloudflare setup and hosting                                :cloudflare:
CLOSED: [2026-08-05]
:PROPERTIES:
:CREATED:  [2026-08-04]
:LIVE:     [2026-08-05] https://laurabmo.com/
:END:

Infrastructure is complete. Domain, DNS, email, hosting, TLS and analytics are all in
place, and the deploy runs itself on push to =main=. Everything remaining in this file
is content.

** DONE Register laurabmo.com                                        :domain:
CLOSED: [2026-08-04]
:PROPERTIES:
:REGISTRAR: Cloudflare
:NS:        kami / patrick .ns.cloudflare.com
:END:

** DONE Email Routing                                                 :email:
CLOSED: [2026-08-04]
MX records live: route1/2/3.mx.cloudflare.net. Inbound only — see the send-as item
below for replies.

** DONE Switch from Pages to Workers static assets                :cloudflare:
CLOSED: [2026-08-04]
:PROPERTIES:
:FILES: wrangler.jsonc, .github/workflows/deploy.yml, .env
:END:
Cloudflare is folding Pages into Workers — Pages is hidden in the dashboard and no
longer gets new features. Workers needs no dashboard setup at all: =wrangler deploy=
creates the Worker on first run. =CF_PAGES_PROJECT= dropped from =.env=; the name now
lives in =wrangler.jsonc=.

** DONE Reissue the API token for Workers                                :ci:
CLOSED: [2026-08-04]
:PROPERTIES:
:TEMPLATE: "Edit Cloudflare Workers"
:END:

** CANCELLED Create the Pages project by hand                     :cloudflare:
CLOSED: [2026-08-04]
Superseded — the Worker is created automatically on first deploy.

** DONE GitHub repository secrets                                        :ci:
CLOSED: [2026-08-04]
=CLOUDFLARE_API_TOKEN= and =CLOUDFLARE_ACCOUNT_ID=, as Secrets not Variables.

** DONE First deploy                                              :cloudflare:
CLOSED: [2026-08-04]
:PROPERTIES:
:URL:     https://laurabmo.laurea987.workers.dev
:ASSETS:  62
:VERSION: 472d0ed5-7e4c-47d0-8399-20693753f2a3
:END:
First attempt failed: =cloudflare/wrangler-action= defaults to wrangler 3.90.0, which
predates assets-only Workers and errors with "Missing entry-point". Fixed by pinning
=WRANGLER_VERSION= in =.env=.

Verified live: all 11 routes 200, exactly one analytics beacon, =katex.css= only on
the maths pages and not on the home page, both stylesheets carrying SRI, fonts 200.

** DONE Find the Worker in the dashboard                          :cloudflare:
CLOSED: [2026-08-05]
The Worker was there all along; the dashboard was showing a different account. An
empty *Workers & Pages* list is an account-selector problem, not a missing Worker.

** DONE Attach laurabmo.com to the Worker                            :domain:
CLOSED: [2026-08-05]
:PROPERTIES:
:PATH: Workers & Pages → laurabmo → Settings → Domains & Routes → Add → Custom Domain
:END:
*The site is live at https://laurabmo.com/.* Verified: DNS resolves to Cloudflare,
HTTP/2, all seven checked routes 200, exactly one analytics beacon, valid certificate.

** DONE Always Use HTTPS                                            :security:
CLOSED: [2026-08-05]
Verified: =http://laurabmo.com/= now redirects to =https://laurabmo.com/=.
HSTS is still available on the same page if wanted later.

** DONE Add www.laurabmo.com                                          :domain:
CLOSED: [2026-08-05]
=https://www.laurabmo.com/= returns 200.

** DONE Paste the Web Analytics token                             :analytics:
CLOSED: [2026-08-04]
:PROPERTIES:
:FILE:     config/_default/params.toml → cloudflareAnalyticsToken
:SITE_TAG: db18c92cdf8046bb80729b6773e190bf  (API only, unused here)
:END:
The beacon takes the *site token*, not the site tag. Verified: emitted on all 18
pages in production, absent in development.

** DONE Turn OFF Web Analytics auto-install                       :analytics:
CLOSED: [2026-08-04]
Confirmed empirically on the live site: exactly one =cloudflareinsights= script tag.
Two would mean injection is back on and every visit is counted twice — worth
re-checking if the numbers ever look implausible.

** DONE Stop GitHub Pages republishing                              :github:
CLOSED: [2026-08-05]
:PROPERTIES:
:REPO: LauraBMo/LauraBMo.website.io
:END:
Two stopgaps failed first: unpublishing was undone by the next push, and
=build_type=workflow= held for exactly one push before reverting to =legacy= on its
own and rebuilding. The behaviour was tied to the repo *name* — =<username>.github.io=
means "user site" to GitHub — so only the rename fixed it.

Renamed to =LauraBMo.website.io=, remote updated, Pages disabled, =pages= branch gone.
=https://laurabmo.github.io/= now 404s. The project-site URL may serve from GitHub's
CDN cache briefly before expiring.

** DONE Pin the local Hugo                                            :infra:
CLOSED: [2026-08-05]
=sudo dnf versionlock add hugo=. When upgrading: unlock, upgrade, =make check=, fix
deprecations, bump =.env=, relock — all in one commit.

* DONE Old content                                                  :content:
CLOSED: [2026-08-05]
:PROPERTIES:
:CREATED:  [2026-08-04]
:END:

Merged. Build clean, 19 pages, 3 aliases, all verified in the output.

** DONE =about.md= deleted, superseded
CLOSED: [2026-08-05]
Bio was already in =_index.md=, thesis in =/cv=, and all five papers, the preprint and
four talks in =/research= — verbatim. The rest was commented-out theme boilerplate.
=aliases = ["/about/"]= on the home page keeps inbound links working; =/about= no longer
serves the bibliography.

** DONE =contact.md= kept, trimmed
CLOSED: [2026-08-05]
Email only. The dead Copenhagen postal address was removed rather than replaced with a
guess. Not in the nav — the address is sitewide in the footer.

** DONE Bojos pages moved to =/outreach=
CLOSED: [2026-08-05]
=/bojos2021= → =/outreach/bojos-2021=, same for 2022, aliases at both old paths.
They had no front matter at all; titles and dates now set properly. Maths and the
conditional =katex.css= verified on the moved pages. New =/outreach= section with a
list page, added to the nav between Research and SoftReminder.

** DONE Avatar back on the home page
CLOSED: [2026-08-05]
=avatar.jpg=, with alt text. The =.avatar= CSS rule was already there from the old design.

** TODO [#C] =papersextras/disguised1.md= — leave or relocate           :content:
POV-Ray source for Figure 6 of the Disguised paper, linked from =/research= as
"figure code". Real supplementary material; kept as-is. Only the URL is odd, and moving
it would need an alias.

** DONE Unreferenced files in =static/documents/=                          :content:
CLOSED: [2026-09-20]
=RSME2022.pdf= is the =96120= talk and is now linked from =/research=, and
=BrustengaMoncusiL-posterBssf.pdf= is now linked from the poster entries there. Both were
sitting in the repo reachable by URL but linked from nowhere.

*96120: The degree of the linear orbit of a cubic surface* was dropped in migration
because its link was empty — find the slides or drop it deliberately.

* DONE laurabmo.xyz sweep                                          :domain:
CLOSED: [2026-09-19]
Swept every surface reachable from this machine for the old domain: the repo, the
published pages, GitHub (profile, code search, Pages), ORCID, the App Store, and every
PDF and text file under =~/Dropbox= plus =~/Documents=, =~/Desktop=, =~/Downloads=.
The site was one file; the domain sat in a CV template reused for years, so it had
leaked into everything generated from it.

** DONE =/documents/laurabmocv.pdf= unlinked and deleted              :content:
CLOSED: [2026-09-19]
The 2021 CV PDF carried =laurabmo.xyz= in its header block, next to the dead
=brust@math.ku.dk= address, and =/cv= linked it. Link deleted from =content/cv.md=,
file deleted from =static/documents/=. Recoverable with
=git checkout HEAD -- static/documents/laurabmocv.pdf=; =make check= clean, static
files 42 → 41. *Not committed* — the live site keeps serving it until a deploy.

** DONE 115 occurrences in 50 Dropbox files replaced                  :domain:
CLOSED: [2026-09-19]
Token substitution (=laurabmo.xyz= → =laurabmo.com=), so every wrapper survives:
=https://=, trailing slash, LaTeX =\enspace=, org =[[...]]=. Script at
=/tmp/fix_xyz.sh=, dry-run by default. =progress.org= excluded deliberately — there the
substitution would turn "Establish that laurabmo.xyz is gone" into a false statement.
Verified afterwards: no text file under =~/Dropbox= still matches except this one.

** DONE Two dead =github.io= links in sources                        :domain:
CLOSED: [2026-09-19]
=2pag-curr.tex= and =Math/LocDim/TeX/webTexT.tex= pointed at =laurabmo.github.io=,
404 since the rename above. Both now =laurabmo.com=.

Findings: *no PDF under =~/Dropbox= carries =laurabmo.com=* — 44 carry =laurabmo.xyz=,
4 more carry the dead =laurabmo.github.io=. The sources are fixed, the artifacts are
not. =laurabmo.laurea987.workers.dev= is a second live origin for the site. The one live
pointer left is the ORCID record, below.
Scope: =~/Dropbox=, =~/Documents=, =~/Desktop=, =~/Downloads=, then =~/src= — added in a
second pass after the first missed it, and it found two stale *claims about* the domain in
the SoftReminder repo (the =publish-public-mirror= skill states =laurabmo.xyz= is this
site and returns 522; =docs/archive/ToPublish.org= repeats it). Both are settled now: the
skill was rewritten and committed on SoftReminder =main= (2026-09-20, =f7a36ba=, not
pushed), and the archive is left untouched on purpose, as a record of what was believed
then. Not covered: the Evolution
store at =~/.local/share/evolution=, and =~/.career=, where the old CV workflow lived and
which is now gone.

** TODO [#C] CV artifacts still carry the old domain                  :content:
Nothing to do until a CV is next sent — the sources are correct, so anything regenerated
from here on is right. Worth knowing rather than acting on: 44 PDFs on disk carry
=laurabmo.xyz= and none carries =laurabmo.com=. Sources in =Career/cvs=:
=cv-contracts.tex= (=lualatex= per its own header, twin =cv-contracts.org=),
=cv-pharma.tex=, =cv-ai-engineer.tex=, =cv-ai-math-contracts.tex=. The =Career/Merce= set
came from these, and its =.docx= are derived, not hand-made — see below.
Left alone deliberately: the remaining PDFs (MarieCurie submission, published papers, old
application packets, the UAB teaching CVs). Editing the =MarieCurie= =.tex= means it no
longer reproduces what was submitted; the submitted PDFs stay as the record.

** DONE Restore the CV-to-docx transformer                            :content:
CLOSED: [2026-09-20]
The =.docx= are generated from =Career/cvs/*.tex= by =mcv2docx.py= — it rewrites
moderncv's =\cventry=/=\cvitem= into plain LaTeX that pandoc expands instead of dropping,
keeps the title block, then shells out to pandoc. Written in the career session
=1dfd25a3= on 2026-09-14 into =/tmp/cvbuild/=, which no longer exists, so the script was
reconstructed from that transcript into =Career/tools/mcv2docx.py=.
Verified, expectation stated first: regenerating the Merce CV from =cvs/cv-pharma.tex=
gives text identical in length (7518 chars, 204 lines, both) and differing in exactly one
line — the domain, =laurabmo.xyz= → =laurabmo.com=, which is the source edit made the same
day. One portability fix: the hardcoded =/tmp/cvbuild/_intermediate.tex= is now a fresh
=tempfile.mkdtemp()= per run.
An earlier note here claimed the =.docx= "cannot be rebuilt from LaTeX" — wrong, and
inferred from nothing more than a missing =.tex= beside them.

** DONE Macros pandoc drops in the docx route                       :content:
CLOSED: [2026-09-20]
=\CC= was silently dropped by pandoc, so the generated =.docx= read "Python, , eLisp, SQL,
LaTeX" where the PDF has one more entry. Fixed in =Career/tools/mcv2docx.py= two ways: a
=PLAIN_TEXT= map for macros that stand for literal text (=\CC= -> =C++=), and
=carried_definitions()=, which lifts the preamble's own =\newcommand= definitions into the
intermediate file pandoc reads. Verified: the regenerated Merce CV now reads
"Python, C++, eLisp, SQL, LaTeX", and the only other change against the September original
is the domain.

** TODO [#C] External profiles still pointing at the old domain      :domain:
The domain question itself is closed — =laurabmo.com= is the site, and =.xyz= belongs to a
third party. What remains is other people's pages and logins this machine cannot reach:
- [ ] Tonelli-Cueto's CV lists =laurabmo.xyz= (=tonellicueto.github.io/{cv,cv_es}.html=).
      His site, so an email is the fix.
- [ ] Google Scholar (homepage field), LinkedIn, Mastodon, Bluesky, X, zbMATH,
      ResearchGate, email signatures — none reachable from here, none checked.
- [ ] arXiv — never checked in any sweep.
- [ ] Dead =github.io= links on our own repos: =LauraBMo.website.io='s homepage, and
      =Rank2BynaryForms.jl='s README badges and Documenter =canonical=, all 404. The other
      package docs (=MobiusSphere.jl=, =MobiusSpherePlots.jl=, =SoftReminder=) are live.

* TODO Content                                                      :content:
** DONE CV — a downloadable PDF                                        :content:
CLOSED: [2026-09-20]
Two passes. First the facts were assembled from the CV sources in =Career/cvs= and the
industry CVs in =Career/Merce=, with every claim traced to a file: the UAB title is the
*Substitute Professor* variant of three, referees are not published, repository counts were
dropped (four different numbers across the sources), and an availability line went too
(three conflicting versions, all reading as contract-hunting).
Then the page itself was rejected in favour of a document: =static/documents/cv.tex=
(moderncv, banking style, compiled with =lualatex=) produces =static/documents/cv.pdf=, and
=/cv= is now two lines and the download. A CV is something people save and forward, and as a
web page it was a second copy to keep true. Three pages, last one a third full — compressible
to two by merging the talks and teaching lines, if wanted.
Compile: =lualatex cv.tex= from =static/documents/=. Two gotchas recorded in the file: the
=letters= icon set prints a stray "E" for =\email{}, so the address sits in the contact line
instead; and =\moderncvicons= needs lualatex, not pdflatex, for the accented Catalan.

** TODO [#B] Two claims to settle, now that the CV is a PDF               :content:
- The PDF says "eight years of university teaching". Counting the appointments in its own
  sources (autumn 2016-autumn 2018, 2020-2021, 2024-present) gives nearer six or seven.
  Stated rather than computed; confirm or adjust.
- Neither the CV nor the home page says what Laura is looking for now. The positioning is
  an industry job search, and that sentence is hers to write.

** DONE Research — the full record                                          :content:
CLOSED: [2026-09-20]
Checked ORCID's public API and arXiv first: *nothing post-2022 exists* — ORCID is frozen at
Jan 2022 with the same five papers, arXiv has nothing newer than 2021, so the page stopping
in 2022 was correct for published work, not a gap. What was actually missing was older: a
2019 arXiv preprint (1901.08320, the sibling of the published Waring paper, listed with its
overlap noted), ~14 talks and seminars and 2 posters from 2018-2021, and the SCM/Notícies
dissemination articles.
The =96120= talk is solved: it was given at the Congreso Bienal de la RSME in Ciudad Real,
January 2022, and its material has been in the repo all along as =static/documents/RSME2022.pdf=.
Talk lists come from =Laura-Brustenga_CV-full.pdf=, the fullest source.
Flagged in a comment on the page: arXiv's title for 1808.03062 is "The blow up split sections
family" while the CV says "The straight blow up split section family" — three variants, so the
site's was left alone. The years for SCM/Notícies 48 and 49 were inferred at this point; both
are settled against the issue cover dates in the entry below.

** DONE SCM/Notícies Bits list — corrected and brought to 52         :content:
CLOSED: [2026-09-21]
The list stopped at 49 and carried the pieces as drafted, not as published. Checked against
the issue PDFs on =scm.iec.cat= — its index page lists covers and dates only, no
article-level data, so the PDFs are the only source. Three of the four disagreed with the
site: 46 is "Surfejant superfícies", not "Surfer, surfejant les superfícies"; 47 is Setembre
2020, not 2021; and 49 carries "Divulgació matemàtica en línia", while "TikZ i ganyotes" is a
superseded draft still on disk at =Teaching/Bits/48/49v1/Bits_49.tex=.
Added 50 "Mani'm?" (Des. 2022), 51 "Jocs amb matemàtiques" (Feb. 2023) and 52 "Programant
amb Julia" (Set. 2023); 53 (Set. 2024) has no Bits at all. 51 and 52 are with Odí Soler i
Gibert, so the page no longer credits every piece to Martí Prats Soler.
Each entry now links the issue PDF. The hrefs are *not* constructible from the number —
=N46=–=N50=, then =SCMNoticies-51_WEB_ok=, then =Noticies-52_op-web= — so 53's will need the
same treatment. All seven verified 200 on the live page.
Two layout changes came out of it. =.Summary= ignores front-matter =description= and derives
one from the content instead, which for a page that *is* a list of links renders the link text
run together — =layouts/_partials/pagelist.html= now prefers =description=. And a child-page
list can only sit between two content blocks via a shortcode, which is how the sessions list
got above the Bits section.
Sources: =Teaching/Bits/= stops at 50. The 51-52 sources are kept elsewhere — a title and
content search under =~/Dropbox= finds neither, so do not go looking there for them.

** TODO [#B] SoftReminder — app page + privacy policy
The privacy half is answered: the policy is written, lives beside the app's code on GitHub,
and is the URL the Play listing points at
(=https://github.com/LauraBMo/SoftReminder/blob/main/docs/privacy-policy.md=, checked
reachable 2026-09-20). This section's job is to MOVE it, not draft it: if it is built, the
policy lives at =/softreminder/privacy= and the listing URL changes with it.
The APK is now linked from =/softreminder= as a GitHub Releases link, which resolves today
and starts serving an APK the moment one is published; no release exists yet. Decided
against a copy in =static/= — a binary in git, and a whole-site redeploy per release.
Still to write: what the app does, for whom. Never link =laurabmo.xyz= from this site.

** TODO [#C] Writing — essays; drafts by default, different register allowed
** TODO [#C] =/help= — section does not exist yet and is not in the nav.
   =archetypes/help.md= already defaults new posts to =draft: true=.

* TODO Housekeeping
** DONE Commit the rebuild                                             :git:
CLOSED: [2026-08-04]
:PROPERTIES:
:COMMIT: 2daaf20
:END:
60 files. Not pushed yet — pushing runs =ci.yml=, which should pass; merging to
=main= runs =deploy.yml=, which will fail its upload step until the Cloudflare
side exists.
** DONE Remove =Bash(docker --version)= from =.claude/settings.local.json= :infra:
CLOSED: [2026-08-04]
** CANCELLED Push =rebuild= and open a pull request                    :git:
CLOSED: [2026-09-20]
Superseded: the branch was merged to =main= and has been deploying from it for weeks. No
pull request was ever opened, which was the other half of this item.

** NEXT [#B] Gmail "Send mail as" for laura@laurabmo.com             :email:
:PROPERTIES:
:WHY: Cloudflare Email Routing is inbound only. It has no outbound SMTP — replies
      currently go out from the personal Gmail address, publishing what the site
      deliberately keeps private.
:END:
Send through *Gmail's* SMTP, not Cloudflare's. There is no Cloudflare SMTP server.
- [ ] Enable 2-Step Verification on the Google account, then create an *App Password*
      (16 characters; the normal password will not work)
- [ ] Gmail → Settings → Accounts and Import → Send mail as → Add another email address
- [ ] =laura@laurabmo.com=, and *untick* "Treat as an alias"
- [ ] SMTP =smtp.gmail.com=, port 587, username = full Gmail address, password = App Password
- [ ] Confirm the code Google sends to =laura@laurabmo.com= (Cloudflare forwards it back)

Caveat: mail sent this way is DKIM-signed as =gmail.com=, so it is not DMARC-aligned to
=laurabmo.com=. It delivers fine in practice, but do not publish a strict DMARC policy
for the domain without revisiting this. If deliverability ever becomes a problem, the
upgrade is a real mailbox provider (Zoho, Fastmail, Migadu) or an SMTP relay that signs
as the domain.

** DONE Update the site URL on ORCID                                  :domain:
CLOSED: [2026-09-20]
Any profile still pointing at =laurabmo.xyz= now points at a squatter. ORCID
=0000-0002-0696-7672= carried =http://www.laurabmo.xyz= as its only researcher-url until
[2026-09-20], when it was changed to =http://laurabmo.com=. Verified by re-querying
=https://pub.orcid.org/v3.0/0000-0002-0696-7672/person= — the same endpoint that found it.
The =http://= is fine: it 301s to HTTPS (checked).
** TODO [#C] Decide whether =progress.org= belongs in the repo or in =.gitignore=

* TODO Polish                                                        :design:
** DONE Share cards, canonical, and a real 404                        :hugo:
CLOSED: [2026-09-20]
No page emitted Open Graph or Twitter tags, so links shared on LinkedIn or Slack rendered
as a bare URL. =baseof.html= now emits =og:*=, =twitter:*= and a canonical link, and reads
the card image from =site.Params.ogImage= — blank for now, so nothing ships until an image
exists; =static/avatar.jpg= is 564x736 and a large card wants 1200px wide.
=layouts/404.html= plus =assets.not_found_handling = "404-page"= replaced Cloudflare's bare
default. Verified live: an unmatched path returns 404 with the site's own page.

The card image and icons followed: =static/og-card.png= (1200x630, the site's palette and serif, the mark and the site's own description), =favicon.svg= — the 16x16 pixel mark rebuilt as vector rectangles, so it is crisp at any size instead of a blurry upscale — and =apple-touch-icon.png= (180x180, nearest-neighbour from the same pixels). Wired in =baseof.html=, with =ogImage= set in =params.toml=.
The mark is BMO, from Adventure Time: B(rustenga) M(oncusí), and the character is loved.
That is deliberate and recorded in favicon.svg itself, so a later pass does not "fix" it into
a monogram. Vectorised from the original 16x16 .ico as one <rect> per colour run.
The card is now in SoftReminder's palette — Paper #F6F1E6, Ink #2A2724, clay accent — rather
than the site's own greys, and it carries the share line "Mathematician. Trust the process."
That line is =site.Params.shareDescription=, deliberately separate from =description=: the
latter is also the meta description, where keywords earn their place in search results, while
a card in a chat preview is read by a person. Home is now in the nav too, ahead of CV.

** DONE Take =.Lastmod= from git, ahead of the file mtime               :hugo:
CLOSED: [2026-09-20]
The footer printed the file's mtime, and on a CI checkout that mtime is the checkout time —
so every deploy stamped the current date on every page. Found by diffing the live home page
against a local build: live said =19 Sep 2026=, local said =5 Aug 2026=, byte-identical
otherwise.
It took two commits, and the first one was wrong: =enableGitInfo= alone changed nothing,
because =config.toml= already carried its own =frontmatter.lastmod= chain with
=:fileModTime= ahead of =:git=. =hugo config= prints the resolved chains, which is what
showed it. The chain is now =[':git', 'lastmod', ':fileModTime', ':default']=, with
=:fileModTime= kept only for a file not yet committed.
Verified live: =/= =5 Aug=, =/cv/= =20 Sep=, =/research/= =4 Aug=, each matching that file's
git committer date.

** TODO [#C] Design pass — deliberately deferred through the whole rebuild
** DONE Legibility with CSS disabled                                            :hugo:
CLOSED: [2026-09-20]
Checked the markup and the stylesheet rather than a visual browser render: =main.css=
contains no =content:=, =display: none= or =visibility= rules at all, so nothing meaningful
is carried by CSS; every page has real =header=, =nav=, =ul=, =main= and =footer= elements;
the one image carries alt text; dark mode is =prefers-color-scheme=, no script. A visual
pass in a browser remains undone, which is a weaker claim than this item originally asked
for.

* Someday
:PROPERTIES:
:CREATED:  [2026-08-04]
:END:

** TODO [#C] =brustenga.cat= or =laurabmo.cat=                        :domain:
Both free as of [2026-08-04]. Eligibility is near-certain — the test is membership of
the Catalan linguistic community, and registration is immediate with a declaration of
intended use, no approval queue. The catch is an obligation to publish Catalan content
(reportedly within 6 months) and roughly triple the price of =.com=.
Revisit when =/writing= actually has Catalan material.
*Unverified*: whether partial Catalan content suffices. One email to suport@domini.cat.

** TODO [#C] Weekly CI canary against Hugo =latest=                      :ci:
A scheduled job that builds against =latest= and is allowed to fail: pinned deploys,
plus warning before the next bump bites. Optional.

* Decisions
:PROPERTIES:
:CREATED:  [2026-08-04]
:END:

| Decision              | Chose                    | Over                          | Because                                          |
|-----------------------+--------------------------+-------------------------------+--------------------------------------------------|
| LaTeX                 | build-time KaTeX         | runtime KaTeX, MathJax        | no runtime JS, no CDN, no flash of raw source     |
| Fonts                 | self-hosted woff2        | Google Fonts                  | GDPR — Munich court, 2022                        |
| Analytics             | Cloudflare Web Analytics | none, GA, zone analytics only | cookieless, per-path, bot-filtered, no new party  |
| Beacon                | in-repo partial          | Cloudflare edge injection     | what ships to visitors stays visible in the repo  |
| Host                  | Cloudflare Workers       | GitHub Pages, Cloudflare Pages | Pages is being folded into Workers; no dashboard setup |
| Build                 | GitHub Actions           | Cloudflare build system       | keeps version pin and --panicOnWarning in repo    |
| Domain                | laurabmo.com             | .xyz, .me, .cat               | neutral for job search, email deliverability      |
| Registrar             | Cloudflare               | keep separate                 | user preference; note the concentration risk      |
| Local toolchain       | native Hugo (dnf)        | Docker, Podman                | static binary; containers bought nothing          |
| Hugo version          | pinned, 0.162.1          | latest                        | breakage arrives when you choose, not at random   |
| Site mark             | BMO (Adventure Time)     | a monogram, a photo           | initials of Brustenga Moncusí, and the character is loved |
| CV                    | PDF, minimal /cv         | the CV as a web page          | a CV is saved and forwarded; the page was a second copy to keep true |
| Share preview         | separate shareDescription | reuse the meta description   | keywords earn their place in search; a person reads the card |
